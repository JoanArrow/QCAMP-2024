package QGame;

public class Space {
    public Space() {

    }

    public String getSymbol() {
        return "";
    }

    public Space runOperation(Space input) {
        //useless method, just so the compiler doesnt have a panic attack
        return input;
    }

    public boolean isSuperposition() {
        //another thing so the compiler doesnt yell at me cause I didnt lay it out like I should oops
        return false;
    }
}
